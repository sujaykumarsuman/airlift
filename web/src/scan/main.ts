// Scan (phone relay) entry (served at `/s/{sid}`). Phase 3.
import { VERSION } from "../shared/version";

document.querySelector("#app")!.textContent = `airlift scan ${VERSION} — not implemented yet (Phase 3)`;
