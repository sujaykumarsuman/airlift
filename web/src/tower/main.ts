// Tower dashboard entry (served at `/`). Phase 3.
import { VERSION } from "../shared/version";

document.querySelector("#app")!.textContent = `airlift tower ${VERSION} — not implemented yet (Phase 3)`;
