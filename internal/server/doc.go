// Package server exposes the HTTP API (docs/API.md): session routes, token
// auth, SSE state streams, /ca.crt, request limits, serving of the embedded
// web UI, and the verification stage that runs when a session completes.
package server
